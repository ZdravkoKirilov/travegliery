import { SharingTypePipe } from './sharing-type.pipe';

describe('SharingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SharingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
